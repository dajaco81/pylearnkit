[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pylearnkit"
version = "0.1.0"
description = "Task loading and checking utilities for Python practice notebooks."
requires-python = ">=3.10"

[tool.setuptools]
packages = ["pylearnkit"]
